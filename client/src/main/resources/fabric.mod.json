{
	"schemaVersion": 1,
	"id": "${id}-client",
	"version": "${version}",
	"name": "PolyCog Client",
	"description": "PolyCog's client distribution.",
	"environment": "client",
	"depends": {
		"${id}": "${version}"
	},
	"jars": [
		{
			"file": "META-INF/jars/${common_jar}"
		}
	]
}
